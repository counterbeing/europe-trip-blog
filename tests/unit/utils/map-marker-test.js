import mapMarker from 'europe-trip-blog/utils/map-marker';
import { module, test } from 'qunit';

module('Unit | Utility | map marker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mapMarker();
  assert.ok(result);
});
